#!/usr/bin/make -f

export PATH=$(shell echo $$PATH):/tmp/venvs/meson/bin
export PIPX_HOME=/tmp

%:
	pipx install meson
	./configure.meson build --prefix /usr --libdir /usr/lib
	dh $@ --buildsystem=meson --builddirectory=build

override_dh_makeshlibs:
	dh_makeshlibs -n
